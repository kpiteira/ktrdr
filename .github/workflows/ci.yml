name: CI Pipeline

on:
  push:
    branches: [ main, develop, fix/test-suite-performance ]
  pull_request:
    branches: [ main, develop ]
  workflow_dispatch:

jobs:
  unit-tests-and-quality:
    name: Unit Tests & Code Quality
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v4
    - uses: astral-sh/setup-uv@v3
      with:
        enable-cache: true
        cache-dependency-glob: "uv.lock"
    
    - name: Set up Python
      run: uv python install 3.13
    
    - name: Install dependencies
      run: uv sync --all-extras --dev
    
    - name: Run unit tests with coverage
      run: |
        echo "Running tests in parallel on $(nproc) CPUs"
        make test-coverage-junit
    
    - name: Upload coverage to Codecov
      uses: codecov/codecov-action@v4
      with:
        file: ./coverage.xml
        flags: unittests
        name: codecov-unit
      env:
        CODECOV_TOKEN: ${{ secrets.CODECOV_TOKEN }}
    
    - name: Run quality checks
      run: make quality
    
    - name: Upload test results
      uses: actions/upload-artifact@v4
      if: always()
      with:
        name: unit-test-results
        path: unit-test-results.xml



  test-summary:
    name: Test Summary
    runs-on: ubuntu-latest
    needs: [unit-tests-and-quality]
    if: always()
    
    steps:
    - name: Download test artifacts
      uses: actions/download-artifact@v4
      continue-on-error: true
    
    - name: Test Summary
      run: |
        echo "## Test Results Summary" >> $GITHUB_STEP_SUMMARY
        echo "" >> $GITHUB_STEP_SUMMARY
        
        if [ -f unit-test-results/unit-test-results.xml ]; then
          echo "✅ Unit tests & code quality completed" >> $GITHUB_STEP_SUMMARY
        else
          echo "❌ Unit tests or code quality failed" >> $GITHUB_STEP_SUMMARY
        fi
        
        echo "" >> $GITHUB_STEP_SUMMARY
        echo "**Note**: Integration (\`make test-integration\`) and E2E (\`make test-e2e\`) tests should be run locally." >> $GITHUB_STEP_SUMMARY
        echo "Host service tests requiring real IB Gateway should be run locally with \`make test-host\`." >> $GITHUB_STEP_SUMMARY