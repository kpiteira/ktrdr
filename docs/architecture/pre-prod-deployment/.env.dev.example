# =============================================================================
# KTRDR Local Development Environment Variables
# =============================================================================
# This file is for LOCAL DEVELOPMENT ONLY (Mac, laptop, etc.)
# Copy to .env.dev and fill in values for local testing
#
# HOMELAB DEPLOYMENT DOES NOT USE .env FILES
# - Homelab secrets come from 1Password
# - Deployment CLI (ktrdr deploy) fetches and injects inline
# - See ENV_VARS.md for complete reference
# =============================================================================

# -----------------------------------------------------------------------------
# Database Configuration
# -----------------------------------------------------------------------------
DB_HOST=db
DB_PORT=5432
DB_NAME=ktrdr
DB_USER=ktrdr_dev
DB_PASSWORD=dev_password_change_me

# -----------------------------------------------------------------------------
# Backend API Configuration
# -----------------------------------------------------------------------------
# JWT_SECRET: Generate with: openssl rand -base64 32
JWT_SECRET=dev_jwt_secret_at_least_32_chars_long_12345

KTRDR_API_HOST=0.0.0.0
KTRDR_API_PORT=8000
ENVIRONMENT=development
LOG_LEVEL=DEBUG
SHARED_MOUNT_PATH=/mnt/shared

# -----------------------------------------------------------------------------
# Observability Configuration
# -----------------------------------------------------------------------------
OTLP_ENDPOINT=http://jaeger:4317
GF_ADMIN_PASSWORD=admin

# Enable anonymous Grafana access (dev only!)
GF_AUTH_ANONYMOUS_ENABLED=true
GF_AUTH_ANONYMOUS_ORG_ROLE=Admin

# -----------------------------------------------------------------------------
# Container Registry Configuration
# -----------------------------------------------------------------------------
IMAGE_TAG=latest

# =============================================================================
# Local Development Notes
# =============================================================================
# - Use simple passwords for dev (never use production secrets locally!)
# - All services accessible at localhost (backend:8000, grafana:3000, etc.)
# - For distributed testing, start workers with:
#     docker compose -f docker-compose.dev.yml --profile workers up
#
# SECURITY: This file is for LOCAL DEVELOPMENT ONLY
# - Never use these values in homelab or production
# - .env.dev is in .gitignore (never commit it)
# =============================================================================
